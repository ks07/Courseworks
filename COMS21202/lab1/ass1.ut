1)>> y = 0.2 * ones(10,1) + 0.5 * x + normrnd(0,0.1,10,1)
x = randn(10,1)
scatter(x,y)

2) function [x,y] = questTwo(m, c, n)
    x = randn(n,1);
    e = normrnd(0,0.1,n,1);
    y = c + m * x + e;

    
3) [x,y] = questTwo(0.2,0.5,100)
X = [ones(100,1) x]
>> a=inv((transpose(X) * X)) * transpose(X) * y

