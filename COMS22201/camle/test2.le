{test4: program to test assignment and variables.}
{Already tested: write statements and constants.}
begin
  i := 10023.0;
  write(i);
  writeln;
  a := (i + 2.0)/2.0;
  write(a);
  writeln;
  j := 76.5;
  write(j);
  writeln;
  a := j;
  write(a);
  writeln;
end
